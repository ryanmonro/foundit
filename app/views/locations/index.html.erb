<h1>All Locations Added</h1>
<% if logged_in? %>
<a href="locations/new">Add a Location</a>
<% end %>
<div>
  <% @locations.each do |location| %>
    <a href="/locations/<%= location.id %>"><img src="<%= location.image %>" alt="<%= location.name %>" width="200"></a>
  <% end %>
</div>
