<h1><%= @user.username %></h1>

<% if @user.founds %>
<h2>Found these locations:</h2>
<% @user.founds.each do |found| %>
<!-- url to this location by id -->
  <a href="<%= location_path(found.location.id) %>">
    <img width="400" src="<%= found.location.image.url %>">
  </a>
<% end %>
<% else %>
<h2>No locations found yet</h2>
<% end %>